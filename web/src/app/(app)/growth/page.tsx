'use client'
import { useState, useEffect, useCallback, useRef } from 'react'
import { supabase } from '@/lib/supabase'
type Rec = Record<string, any>
const GOOGLE_REVIEW_URL = 'https://g.page/r/CSSKpJahtMmSEBM/review'

function Toast({ message, type, onClose }: { message: string; type: 'success' | 'error' | 'info'; onClose: () => void }) {
  useEffect(() => { const t = setTimeout(onClose, 4000); return () => clearTimeout(t) }, [onClose])
  const bg = type === 'success' ? 'bg-emerald-500' : type === 'error' ? 'bg-red-500' : 'bg-blue-500'
  return (<div className={`fixed top-4 right-4 z-[100] ${bg} text-white px-5 py-3 rounded-xl shadow-lg flex items-center gap-3`}><span>{message}</span><button onClick={onClose}>x</button></div>)
}

function timeAgo(d: string) {
  if (!d) return 'Never'
  const days = Math.floor((Date.now() - new Date(d).getTime()) / 86400000)
  if (days === 0) return 'Today'; if (days === 1) return 'Yesterday'
  if (days < 30) return `${days}d ago`; if (days < 365) return `${Math.floor(days / 30)}mo ago`
  return `${Math.floor(days / 365)}y ago`
}

function Modal({ title, onClose, children, wide }: { title: string; onClose: () => void; children: React.ReactNode; wide?: boolean }) {
  return (<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm" onClick={onClose}><div className={`bg-[#1a2332] border border-white/10 rounded-2xl p-6 shadow-2xl ${wide ? 'max-w-3xl' : 'max-w-lg'} w-full mx-4 max-h-[85vh] overflow-y-auto`} onClick={e => e.stopPropagation()}><div className="flex justify-between items-center mb-5"><h2 className="text-xl font-bold text-white">{title}</h2><button onClick={onClose} className="text-gray-400 hover:text-white text-xl">x</button></div>{children}</div></div>)
}

function ReportModal({ title, leads, onClose, onOutreach, sending }: { title: string; leads: any[]; onClose: () => void; onOutreach: (id: string, method: string) => void; sending: string | null }) {
  return (<div className="fixed inset-0 z-50 bg-black/80 backdrop-blur-sm" onClick={onClose}><div className="w-full h-full max-w-5xl mx-auto p-4 pt-6 overflow-y-auto" onClick={e => e.stopPropagation()}><div className="flex justify-between items-center mb-6"><h2 className="text-2xl font-bold text-white">{title}</h2><div className="flex items-center gap-3"><span className="text-sm text-gray-400">{leads.length} leads found</span><button onClick={onClose} className="text-gray-400 hover:text-white bg-white/10 rounded-full w-8 h-8 flex items-center justify-center">x</button></div></div><div className="grid gap-3">{leads.map((l: any, i: number) => { const n = typeof l.notes === 'string' ? (() => { try { return JSON.parse(l.notes) } catch { return l } })() : (l.notes || l); return (<div key={l.id || i} className="bg-[#1a2332] border border-white/10 rounded-xl p-4"><div className="flex items-start justify-between gap-4"><div className="flex-1 min-w-0"><div className="flex items-center gap-2 mb-1"><span className="font-bold text-white">{l.name || n.name || n.reviewer_name || 'Unknown'}</span>{(n.confidence || l.confidence) && <span className={`text-[10px] px-1.5 py-0.5 rounded font-semibold ${(n.confidence || l.confidence) === 'high' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-yellow-500/20 text-yellow-400'}`}>{n.confidence || l.confidence}</span>}{n.fleet_score && <span className="text-[10px] px-1.5 py-0.5 rounded bg-blue-500/20 text-blue-400 font-semibold">{n.fleet_score}/10</span>}{n.urgency && <span className={`text-[10px] px-1.5 py-0.5 rounded font-semibold ${n.urgency === 'high' ? 'bg-red-500/20 text-red-400' : 'bg-yellow-500/20 text-yellow-400'}`}>{n.urgency}</span>}</div><div className="grid grid-cols-2 gap-x-4 gap-y-1 text-xs mb-2">{(l.phone || n.phone) && <span className="text-blue-400">Phone: {l.phone || n.phone}</span>}{(l.email || n.email) && <span className="text-gray-300">Email: {l.email || n.email}</span>}{n.business_type && <span className="text-gray-400">Type: {n.business_type}</span>}{n.platform && <span className="text-purple-400">Platform: {n.platform}</span>}{n.competitor_name && <span className="text-orange-400">From: {n.competitor_name}</span>}{n.estimated_vehicles && <span className="text-emerald-400">Vehicles: {n.estimated_vehicles}</span>}{n.annual_value_estimate && <span className="text-emerald-400">Value: {n.annual_value_estimate}</span>}{(l.service_needed || n.service_needed || n.service_issue) && <span className="text-gray-300">Need: {l.service_needed || n.service_needed || n.service_issue}</span>}</div>{(n.review_snippet || n.post_snippet) && <p className="text-xs text-gray-400 italic mb-2">"{n.review_snippet || n.post_snippet}"</p>}{(n.suggested_message || n.outreach_pitch) && <p className="text-xs text-emerald-300 bg-emerald-500/10 rounded-lg p-2 mb-2">{n.suggested_message || n.outreach_pitch}</p>}</div><div className="flex flex-col gap-1.5 shrink-0">{l.id && <><button onClick={() => onOutreach(l.id, 'sms')} disabled={sending === l.id || !(l.phone || n.phone)} className={`px-3 py-1.5 rounded-lg text-xs font-semibold transition-all ${(l.phone || n.phone) ? 'bg-blue-600 text-white hover:bg-blue-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'SMS'}</button><button onClick={() => onOutreach(l.id, 'email')} disabled={sending === l.id || !(l.email || n.email)} className={`px-3 py-1.5 rounded-lg text-xs font-semibold transition-all ${(l.email || n.email) ? 'bg-purple-600 text-white hover:bg-purple-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'Email'}</button><button onClick={() => onOutreach(l.id, 'ai_call')} disabled={sending === l.id || !(l.phone || n.phone)} className={`px-3 py-1.5 rounded-lg text-xs font-semibold transition-all ${(l.phone || n.phone) ? 'bg-emerald-600 text-white hover:bg-emerald-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'AI Call'}</button></>}</div></div></div>) })}</div></div></div>)
}

type Tab = 'followups' | 'reviews' | 'referrals' | 'leads' | 'social' | 'capture' | 'intel' | 'ads'
const TABS: { key: Tab; label: string; icon: string }[] = [
  { key: 'followups', label: 'Follow-ups', icon: '\ud83d\udd01' },
  { key: 'reviews', label: 'Reviews', icon: '\u2b50' },
  { key: 'referrals', label: 'Referrals', icon: '\ud83e\udd1d' },
  { key: 'leads', label: 'Lead Gen', icon: '\ud83c\udfaf' },
  { key: 'social', label: 'Social Post', icon: '\ud83d\udcf1' },
  { key: 'capture', label: 'Capture', icon: '\ud83d\udcde' },
  { key: 'intel', label: 'Smart Search', icon: '\ud83d\udd0d' },
  { key: 'ads', label: 'Ads', icon: '\ud83d\udce2' },
]

export default function GrowthPage() {
  const [tab, setTab] = useState<Tab>('followups')
  const [customers, setCustomers] = useState<Rec[]>([])
  const [referrals, setReferrals] = useState<Rec[]>([])
  const [leads, setLeads] = useState<Rec[]>([])
  const [campaigns, setCampaigns] = useState<Rec[]>([])
  const [activityLog, setActivityLog] = useState<Rec[]>([])
  const [loading, setLoading] = useState(true)
  const [sending, setSending] = useState<string | null>(null)
  const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null)
  const [scanning, setScanning] = useState(false)
  const [scanResults, setScanResults] = useState<any>(null)
  const [captureName, setCaptureName] = useState('')
  const [capturePhone, setCapturePhone] = useState('')
  const [captureService, setCaptureService] = useState('')
  const [captureSource, setCaptureSource] = useState('walk-in')
  const [showImportModal, setShowImportModal] = useState(false)
  const [importName, setImportName] = useState('')
  const [importPhone, setImportPhone] = useState('')
  const [importService, setImportService] = useState('')
  const [importSource, setImportSource] = useState('manual')
  const [importNotes, setImportNotes] = useState('')
  const [leadSearch, setLeadSearch] = useState('')
  const [showAdsModal, setShowAdsModal] = useState(false)
  const [adsPlatform, setAdsPlatform] = useState<'google' | 'facebook'>('google')
  const [adsCampaignName, setAdsCampaignName] = useState('')
  const [adsBudget, setAdsBudget] = useState('20')
  const [adsKeywords, setAdsKeywords] = useState('oil change Houston, auto repair near me')
  const [adsSaving, setAdsSaving] = useState(false)
  const [postText, setPostText] = useState('')
  const [postPlatforms, setPostPlatforms] = useState(['facebook', 'google'])
  const [postMedia, setPostMedia] = useState<File[]>([])
  const [posting, setPosting] = useState(false)
  const [postHistory, setPostHistory] = useState<Rec[]>([])
  const fileInputRef = useRef<HTMLInputElement>(null)
  const [intelQuery, setIntelQuery] = useState('')
  const [intelResults, setIntelResults] = useState<any>(null)
  const [intelSearching, setIntelSearching] = useState(false)
  const [scanningCompetitor, setScanningCompetitor] = useState(false)
  const [scanningSocial, setScanningSocial] = useState(false)
  const [scanningFleet, setScanningFleet] = useState(false)
  const [showLeadDetail, setShowLeadDetail] = useState<any>(null)
  const [aiMode, setAiMode] = useState(false)
  const [reportModal, setReportModal] = useState<{ title: string; leads: any[] } | null>(null)
  const [outreachHistory, setOutreachHistory] = useState<Rec[]>([])

  const notify = useCallback((message: string, type: 'success' | 'error' | 'info' = 'info') => { setToast({ message, type }) }, [])
  const load = useCallback(async () => {
    setLoading(true)
    try {
      const [c, r, l, camp, acts, posts, oh] = await Promise.all([supabase.from('customers').select('*').order('created_at', { ascending: false }), supabase.from('referrals').select('*').order('created_at', { ascending: false }), supabase.from('leads').select('*').order('created_at', { ascending: false }), supabase.from('growth_campaigns').select('*').order('created_at', { ascending: false }), supabase.from('growth_activity').select('*').order('created_at', { ascending: false }).limit(50), supabase.from('social_posts').select('*').order('created_at', { ascending: false }).limit(20), supabase.from('outreach_history').select('*').order('created_at', { ascending: false }).limit(50)])
      setCustomers(c.data || []); setReferrals(r.data || []); setLeads(l.data || []); setCampaigns(camp.data || []); setActivityLog(acts.data || []); setPostHistory(posts.data || []); setOutreachHistory(oh.data || [])
    } catch { notify('Failed to load', 'error') }
    setLoading(false)
  }, [notify])
  useEffect(() => { load() }, [load])

  const staleCustomers = customers.filter(c => { if (!c.last_visit && !c.created_at) return false; return (Date.now() - new Date(c.last_visit || c.created_at).getTime()) > 180 * 86400000 })
  const logActivity = async (action: string, target: string, details: string, status: string) => { await supabase.from('growth_activity').insert({ action, target, details, status, created_at: new Date().toISOString() }) }
  const sendSms = async (to: string, message: string) => { const res = await fetch('/api/send-sms', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ to, message }) }); if (!res.ok) throw new Error('SMS failed'); return res.json() }

  const doOutreach = async (leadId: string, method: string) => {
    setSending(leadId)
    try {
      const res = await fetch('/api/growth/outreach', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ lead_id: leadId, method, ai_mode: aiMode }) })
      const d = await res.json()
      if (d.success) { notify(`${method.toUpperCase()} sent successfully!`, 'success'); await load() }
      else notify(d.error || 'Outreach failed', 'error')
    } catch { notify('Outreach failed', 'error') }
    setSending(null)
  }

  const sendFollowUp = async (c: Rec) => { if (!c.phone) return notify('No phone number', 'error'); setSending(c.id); try { await sendSms(c.phone, `Hi ${c.name?.split(' ')[0] || 'there'}! It's Alpha International Auto Center. Time for a checkup? Reply YES or call (713) 663-6979!`); await supabase.from('customers').update({ last_contact: new Date().toISOString() }).eq('id', c.id); await logActivity('follow_up_sms', c.name, `Sent to ${c.phone}`, 'sent'); notify(`Follow-up sent to ${c.name}`, 'success'); await load() } catch { notify('Failed to send', 'error') } setSending(null) }
  const bulkFollowUp = async () => { const eligible = staleCustomers.filter(c => c.phone); if (!eligible.length) return notify('No customers with phone numbers', 'error'); if (!confirm(`Send follow-up to ${eligible.length} customers?`)) return; let sent = 0; for (const c of eligible) { try { await sendFollowUp(c); sent++ } catch {} } notify(`Sent ${sent} of ${eligible.length} follow-ups`, 'success') }
  const requestReview = async (c: Rec) => { if (!c.phone) return notify('No phone number', 'error'); setSending(c.id); try { await sendSms(c.phone, `Hi ${c.name?.split(' ')[0]}! Thanks for choosing Alpha International. We'd love your feedback! ${GOOGLE_REVIEW_URL}`); await supabase.from('customers').update({ review_requested: new Date().toISOString() }).eq('id', c.id); await logActivity('review_request', c.name, `Sent to ${c.phone}`, 'sent'); notify(`Review request sent to ${c.name}`, 'success'); await load() } catch { notify('Failed to send', 'error') } setSending(null) }
  const generateReferral = async (c: Rec) => { const code = `ALPHA-${c.name?.split(' ')[0]?.toUpperCase() || 'REF'}-${Math.random().toString(36).slice(2, 6).toUpperCase()}`; await supabase.from('referrals').insert({ customer_id: c.id, customer_name: c.name, code, discount_percent: 10, uses: 0 }); if (c.phone) await sendSms(c.phone, `Hey ${c.name?.split(' ')[0]}! Share code ${code} with friends - they get 10% off, you get $25 credit!`); await logActivity('referral_created', c.name, `Code: ${code}`, 'active'); notify(`Referral code ${code} created`, 'success'); await load() }
  const captureLead = async () => { if (!captureName.trim()) return notify('Name is required', 'error'); await supabase.from('leads').insert({ name: captureName.trim(), phone: capturePhone.trim() || null, service_needed: captureService.trim() || null, source: captureSource, status: 'new', follow_up_date: new Date(Date.now() + 86400000).toISOString().split('T')[0] }); await logActivity('lead_captured', captureName.trim(), `Source: ${captureSource}`, 'new'); setCaptureName(''); setCapturePhone(''); setCaptureService(''); notify('Lead captured!', 'success'); await load() }
  const followUpLead = async (lead: Rec) => { if (!lead.phone) return notify('No phone for this lead', 'error'); setSending(lead.id); try { const notes = lead.notes ? (typeof lead.notes === 'string' ? JSON.parse(lead.notes) : lead.notes) : {}; const msg = notes.suggested_message || `Hi ${lead.name?.split(' ')[0] || 'there'}! This is Alpha International Auto Center. You inquired about ${lead.service_needed || 'auto repair'}. Ready to schedule? Call (713) 663-6979!`; await sendSms(lead.phone, msg); await supabase.from('leads').update({ status: 'contacted', last_contact: new Date().toISOString() }).eq('id', lead.id); await logActivity('lead_followup', lead.name, `SMS sent to ${lead.phone}`, 'contacted'); notify(`Followed up with ${lead.name}`, 'success'); await load() } catch { notify('Failed to send', 'error') } setSending(null) }
  const importLead = async () => { if (!importName.trim()) return notify('Name is required', 'error'); await supabase.from('leads').insert({ name: importName.trim(), phone: importPhone.trim() || null, service_needed: importService.trim() || null, source: importSource, notes: importNotes.trim() || null, status: 'new', follow_up_date: new Date(Date.now() + 86400000).toISOString().split('T')[0] }); setImportName(''); setImportPhone(''); setImportService(''); setImportNotes(''); setShowImportModal(false); notify('Lead imported!', 'success'); await load() }

  const aiScanCompetitors = async () => { setScanningCompetitor(true); try { const r = await fetch('/api/growth/ai-competitor-leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ city: 'Houston TX' }) }); const d = await r.json(); if (d.leads?.length) { const freshLeads = await supabase.from('leads').select('*').eq('source', 'ai-competitor-scan').order('created_at', { ascending: false }).limit(d.leads.length); setReportModal({ title: 'Competitor Customer Leads', leads: freshLeads.data || [] }) } notify(`AI found ${d.total_leads || 0} unhappy competitor customers!`, 'success'); await load() } catch { notify('AI competitor scan failed', 'error') } setScanningCompetitor(false) }
  const aiScanSocial = async () => { setScanningSocial(true); try { const r = await fetch('/api/growth/ai-social-leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ city: 'Houston TX' }) }); const d = await r.json(); if (d.leads?.length) { const freshLeads = await supabase.from('leads').select('*').eq('source', 'ai-social-scan').order('created_at', { ascending: false }).limit(d.leads.length); setReportModal({ title: 'Social Media Leads', leads: freshLeads.data || [] }) } notify(`AI found ${d.total_leads || 0} people needing auto repair!`, 'success'); await load() } catch { notify('AI social scan failed', 'error') } setScanningSocial(false) }
  const aiScanFleet = async () => { setScanningFleet(true); try { const r = await fetch('/api/growth/ai-fleet-leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ city: 'Houston TX' }) }); const d = await r.json(); if (d.leads?.length) { const freshLeads = await supabase.from('leads').select('*').eq('source', 'ai-fleet-scan').order('created_at', { ascending: false }).limit(d.leads.length); setReportModal({ title: 'Fleet Business Leads', leads: freshLeads.data || [] }) } notify(`AI found ${d.total_leads || 0} fleet businesses!`, 'success'); await load() } catch { notify('AI fleet scan failed', 'error') } setScanningFleet(false) }
  const smartSearch = async () => { if (!intelQuery.trim()) return notify('Enter a name or business to search', 'error'); setIntelSearching(true); setIntelResults(null); try { const r = await fetch('/api/growth/smart-search', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ query: intelQuery.trim() }) }); const d = await r.json(); setIntelResults(d); notify('Search complete!', 'success') } catch { notify('Search failed', 'error') } setIntelSearching(false) }
  const createSocialPost = async () => { if (!postText.trim()) return notify('Write something to post', 'error'); if (!postPlatforms.length) return notify('Select at least one platform', 'error'); setPosting(true); try { const r = await fetch('/api/growth/social-post', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: postText.trim(), platforms: postPlatforms }) }); const d = await r.json(); await logActivity('social_post', postPlatforms.join(', '), postText.slice(0, 60), d.success ? 'posted' : 'draft'); setPostText(''); setPostMedia([]); notify(d.message || 'Post created!', 'success'); await load() } catch { notify('Post failed', 'error') } setPosting(false) }
  const saveAdCampaign = async () => { if (!adsCampaignName.trim()) return notify('Campaign name required', 'error'); setAdsSaving(true); await supabase.from('growth_campaigns').insert({ name: adsCampaignName.trim(), platform: adsPlatform, objective: 'leads', budget_per_day: parseFloat(adsBudget) || 20, keywords: adsKeywords, status: 'draft', spend: 0 }); setAdsCampaignName(''); setAdsBudget('20'); setAdsSaving(false); setShowAdsModal(false); notify('Campaign saved!', 'success'); await load() }

  const filteredLeads = leads.filter(l => !leadSearch || l.name?.toLowerCase().includes(leadSearch.toLowerCase()) || l.phone?.includes(leadSearch))
  const card = 'bg-[#1a2332] border border-white/10 rounded-2xl p-5 shadow-lg'
  const btn = 'px-4 py-2.5 rounded-xl text-sm font-semibold transition-all cursor-pointer'
  const btnP = `${btn} bg-blue-600 text-white hover:bg-blue-500`
  const btnS = `${btn} bg-white/5 text-gray-300 hover:bg-white/10 border border-white/10`
  const btnG = `${btn} bg-emerald-600 text-white hover:bg-emerald-500`
  const inp = 'w-full bg-[#0f1923] border border-white/10 rounded-xl px-4 py-2.5 text-sm text-white placeholder-gray-500 focus:ring-2 focus:ring-blue-500 outline-none'
  const lbl = 'text-xs font-semibold text-gray-400 uppercase tracking-wider mb-1.5 block'
  const parseNotes = (n: any) => { try { return typeof n === 'string' ? JSON.parse(n) : (n || {}) } catch { return {} } }

  return (<div>
{toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}
{reportModal && <ReportModal title={reportModal.title} leads={reportModal.leads} onClose={() => setReportModal(null)} onOutreach={doOutreach} sending={sending} />}
{showImportModal && <Modal title="Import Lead" onClose={() => setShowImportModal(false)}><div className="space-y-4"><div><label className={lbl}>Name *</label><input className={inp} value={importName} onChange={e => setImportName(e.target.value)} placeholder="John Smith" /></div><div><label className={lbl}>Phone</label><input className={inp} value={importPhone} onChange={e => setImportPhone(e.target.value)} placeholder="(713) 555-0000" /></div><div><label className={lbl}>Service</label><input className={inp} value={importService} onChange={e => setImportService(e.target.value)} placeholder="Oil change, brakes..." /></div><div><label className={lbl}>Notes</label><textarea className={inp} rows={2} value={importNotes} onChange={e => setImportNotes(e.target.value)} /></div><button onClick={importLead} className={`${btnG} w-full`}>Save Lead</button></div></Modal>}
{showAdsModal && <Modal title={`Create ${adsPlatform === 'google' ? 'Google' : 'Facebook'} Campaign`} onClose={() => setShowAdsModal(false)}><div className="space-y-4"><div className="flex gap-2"><button onClick={() => setAdsPlatform('google')} className={`flex-1 py-2 rounded-xl text-sm font-semibold border-2 ${adsPlatform === 'google' ? 'border-blue-500 bg-blue-500/10 text-blue-400' : 'border-white/10 text-gray-400'}`}>Google Ads</button><button onClick={() => setAdsPlatform('facebook')} className={`flex-1 py-2 rounded-xl text-sm font-semibold border-2 ${adsPlatform === 'facebook' ? 'border-blue-500 bg-blue-500/10 text-blue-400' : 'border-white/10 text-gray-400'}`}>Facebook Ads</button></div><div><label className={lbl}>Campaign Name *</label><input className={inp} value={adsCampaignName} onChange={e => setAdsCampaignName(e.target.value)} placeholder="Spring Special" /></div><div><label className={lbl}>Daily Budget ($)</label><input className={inp} type="number" min="5" value={adsBudget} onChange={e => setAdsBudget(e.target.value)} /></div><div className="p-3 bg-yellow-500/10 rounded-xl text-xs text-yellow-300">Saves as draft. Activate from your ads dashboard.</div><button onClick={saveAdCampaign} disabled={adsSaving} className={`${btnP} w-full`}>{adsSaving ? 'Saving...' : 'Save Draft'}</button></div></Modal>}

<div className="flex items-center justify-between mb-8"><div><h1 className="text-3xl font-bold text-white">Growth Center</h1><p className="text-gray-400 text-sm mt-1">Acquire, retain, and grow your customer base</p></div><div className="flex items-center gap-3"><div className="flex items-center gap-2 bg-[#0f1923] rounded-xl px-4 py-2 border border-white/10"><span className="text-xs text-gray-400">Manual</span><button onClick={() => setAiMode(!aiMode)} className={`relative w-12 h-6 rounded-full transition-all ${aiMode ? 'bg-emerald-600' : 'bg-gray-600'}`}><span className={`absolute top-0.5 ${aiMode ? 'left-6' : 'left-0.5'} w-5 h-5 rounded-full bg-white transition-all shadow`} /></button><span className={`text-xs font-semibold ${aiMode ? 'text-emerald-400' : 'text-gray-400'}`}>AI Autopilot</span></div><button onClick={load} className={btnS} disabled={loading}>{loading ? 'Loading...' : 'Refresh'}</button></div></div>

<div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">{[{ label: 'Customers', value: customers.length, color: 'text-white' }, { label: 'Need Follow-up', value: staleCustomers.length, color: 'text-amber-400' }, { label: 'Active Referrals', value: referrals.length, color: 'text-emerald-400' }, { label: 'Open Leads', value: leads.filter(l => l.status !== 'converted').length, color: 'text-blue-400' }].map((s, i) => (<div key={i} className={card}><p className="text-xs font-semibold text-gray-400 uppercase">{s.label}</p><p className={`text-3xl font-bold mt-2 ${s.color}`}>{s.value}</p></div>))}</div>

{activityLog.length > 0 && <div className={`${card} mb-8`}><h3 className="font-bold text-white mb-3">Recent Activity</h3><div className="space-y-1 max-h-32 overflow-y-auto">{activityLog.slice(0, 5).map((a, i) => (<div key={i} className="flex items-center justify-between text-xs py-1.5 border-b border-white/5"><span className="text-gray-300">{a.action?.replace(/_/g, ' ')} - <span className="text-white font-medium">{a.target}</span></span><span className="text-gray-500">{timeAgo(a.created_at)}</span></div>))}</div></div>}

<div className="flex gap-1 mb-8 bg-[#0f1923] p-1.5 rounded-2xl overflow-x-auto">{TABS.map(t => (<button key={t.key} onClick={() => setTab(t.key)} className={`flex items-center gap-2 px-5 py-3 rounded-xl text-sm font-semibold whitespace-nowrap transition-all ${tab === t.key ? 'bg-blue-600 text-white shadow-lg' : 'text-gray-400 hover:text-white hover:bg-white/5'}`}><span>{t.icon}</span> {t.label}</button>))}</div>

{tab === 'followups' && <div className={card}><div className="flex items-center justify-between mb-5"><div><h2 className="text-xl font-bold text-white">Automated Follow-ups</h2><p className="text-sm text-gray-400 mt-1">Customers inactive 6+ months</p></div>{staleCustomers.filter(c => c.phone).length > 0 && <button onClick={bulkFollowUp} className={btnP}>Send All ({staleCustomers.filter(c => c.phone).length})</button>}</div>{staleCustomers.length === 0 ? <div className="text-center py-12"><p className="text-gray-400">All customers are active!</p></div> : <div className="space-y-2">{staleCustomers.map(c => (<div key={c.id} className="flex items-center justify-between p-4 bg-[#0f1923] rounded-xl"><div><p className="font-semibold text-white">{c.name}</p><p className="text-xs text-gray-500">{c.phone || 'No phone'} - Last: {timeAgo(c.last_visit || c.created_at)}</p></div><button onClick={() => sendFollowUp(c)} disabled={sending === c.id || !c.phone} className={c.phone ? btnP : btnS}>{sending === c.id ? 'Sending...' : 'Send Text'}</button></div>))}</div>}</div>}

{tab === 'reviews' && <div className={card}><div className="mb-5"><h2 className="text-xl font-bold text-white">Reviews & Reputation</h2><p className="text-sm text-gray-400 mt-1">Ask happy customers for Google reviews</p></div><div className="space-y-2">{customers.slice(0, 50).map(c => (<div key={c.id} className="flex items-center justify-between p-4 bg-[#0f1923] rounded-xl"><div><p className="font-semibold text-white">{c.name}</p><p className="text-xs text-gray-500">{c.phone || 'No phone'}{c.review_requested ? ` - Requested ${timeAgo(c.review_requested)}` : ''}</p></div><button onClick={() => requestReview(c)} disabled={sending === c.id || !c.phone} className={c.review_requested ? btnS : btnG}>{sending === c.id ? 'Sending...' : c.review_requested ? 'Resend' : 'Ask for Review'}</button></div>))}</div></div>}

{tab === 'referrals' && <div className="space-y-6"><div className={card}><div className="mb-5"><h2 className="text-xl font-bold text-white">Referral System</h2><p className="text-sm text-gray-400 mt-1">Generate codes. They share, everyone saves.</p></div><div className="space-y-2">{customers.slice(0, 30).map(c => { const ex = referrals.find(r => r.customer_id === c.id); return (<div key={c.id} className="flex items-center justify-between p-4 bg-[#0f1923] rounded-xl"><div><p className="font-semibold text-white">{c.name}</p>{ex ? <p className="text-xs text-emerald-400 font-mono">{ex.code} - {ex.uses || 0} uses</p> : <p className="text-xs text-gray-500">{c.phone || 'No phone'}</p>}</div>{ex ? <span className="text-xs bg-emerald-500/20 text-emerald-400 px-3 py-1.5 rounded-lg font-semibold">Active</span> : <button onClick={() => generateReferral(c)} className={btnP}>Generate Code</button>}</div>) })}</div></div></div>}

{tab === 'leads' && <div className="space-y-6"><div className={card}><div className="flex items-center justify-between mb-5"><div><h2 className="text-xl font-bold text-white">AI Lead Generation</h2><p className="text-sm text-gray-400 mt-1">AI finds customers. Click a button - a full report opens with outreach options.</p></div>{aiMode && <span className="text-xs bg-emerald-500/20 text-emerald-400 px-3 py-1.5 rounded-lg font-semibold">AI Autopilot ON</span>}</div><div className="grid md:grid-cols-3 gap-4 mb-5"><button className={`p-4 rounded-2xl border-2 border-dashed text-left transition-all ${scanningCompetitor ? 'border-orange-500 bg-orange-500/10' : 'border-orange-500/30 hover:border-orange-500 hover:bg-orange-500/5'}`} onClick={aiScanCompetitors} disabled={scanningCompetitor}><div className="text-2xl mb-2">{scanningCompetitor ? '\u23f3' : '\ud83d\udd25'}</div><h3 className="font-bold text-white text-sm">Steal Competitor Customers</h3><p className="text-xs text-gray-400 mt-1">{scanningCompetitor ? 'AI scanning bad reviews...' : 'Scans competitor reviews, finds unhappy customers with outreach messages'}</p></button><button className={`p-4 rounded-2xl border-2 border-dashed text-left transition-all ${scanningSocial ? 'border-blue-500 bg-blue-500/10' : 'border-blue-500/30 hover:border-blue-500 hover:bg-blue-500/5'}`} onClick={aiScanSocial} disabled={scanningSocial}><div className="text-2xl mb-2">{scanningSocial ? '\u23f3' : '\ud83d\udcf1'}</div><h3 className="font-bold text-white text-sm">Find Social Media Leads</h3><p className="text-xs text-gray-400 mt-1">{scanningSocial ? 'AI scanning Facebook, Reddit...' : 'Finds people posting about car problems in Houston'}</p></button><button className={`p-4 rounded-2xl border-2 border-dashed text-left transition-all ${scanningFleet ? 'border-emerald-500 bg-emerald-500/10' : 'border-emerald-500/30 hover:border-emerald-500 hover:bg-emerald-500/5'}`} onClick={aiScanFleet} disabled={scanningFleet}><div className="text-2xl mb-2">{scanningFleet ? '\u23f3' : '\ud83d\ude9a'}</div><h3 className="font-bold text-white text-sm">Find Fleet Businesses</h3><p className="text-xs text-gray-400 mt-1">{scanningFleet ? 'AI scanning 25+ business types...' : '25+ categories: plumbers, HVAC, delivery, churches, food trucks & more'}</p></button></div><div className="flex gap-3"><input className={inp} placeholder="Search leads..." value={leadSearch} onChange={e => setLeadSearch(e.target.value)} /><button className={btnS} onClick={() => { setImportSource('manual'); setShowImportModal(true) }}>+ Manual</button></div></div>

<div className={card}><div className="flex items-center justify-between mb-4"><h3 className="font-bold text-white">All Leads ({filteredLeads.length})</h3><div className="flex gap-2"><button onClick={() => { const aiLeads = filteredLeads.filter(l => ['ai-competitor-scan','ai-social-scan','ai-fleet-scan'].includes(l.source)); if (aiLeads.length) setReportModal({ title: 'All AI Leads', leads: aiLeads }) }} className={btnS}>View Report</button></div></div>{filteredLeads.length === 0 ? <p className="text-gray-500 text-center py-8">No leads yet. Click an AI button above to start!</p> : <div className="space-y-2">{filteredLeads.map(l => { const n = parseNotes(l.notes); const isAI = ['ai-competitor-scan', 'ai-social-scan', 'ai-fleet-scan'].includes(l.source); return (<div key={l.id} className="flex items-center justify-between p-4 bg-[#0f1923] rounded-xl"><div className="flex-1 min-w-0"><div className="flex items-center gap-2"><p className="font-semibold text-white truncate">{l.name}</p>{isAI && <span className="text-[10px] px-1.5 py-0.5 rounded bg-purple-500/20 text-purple-400 font-semibold">AI</span>}<span className={`text-[10px] px-1.5 py-0.5 rounded font-semibold ${l.status === 'new' ? 'bg-blue-500/20 text-blue-400' : l.status === 'contacted' ? 'bg-yellow-500/20 text-yellow-400' : 'bg-emerald-500/20 text-emerald-400'}`}>{l.status}</span></div><p className="text-xs text-gray-500">{l.source?.replace(/-/g, ' ')} - {l.service_needed || 'General'}</p></div><div className="flex gap-1.5 ml-3"><button onClick={() => doOutreach(l.id, 'sms')} disabled={sending === l.id || !l.phone} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${l.phone ? 'bg-blue-600 text-white hover:bg-blue-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'SMS'}</button><button onClick={() => doOutreach(l.id, 'email')} disabled={sending === l.id || !l.email} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${l.email ? 'bg-purple-600 text-white hover:bg-purple-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'Email'}</button><button onClick={() => doOutreach(l.id, 'ai_call')} disabled={sending === l.id || !l.phone} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${l.phone ? 'bg-emerald-600 text-white hover:bg-emerald-500' : 'bg-white/5 text-gray-500'}`}>{sending === l.id ? '...' : 'AI Call'}</button></div></div>) })}</div>}</div></div>}

{tab === 'social' && <div className="space-y-6"><div className={card}><h2 className="text-xl font-bold text-white mb-5">Create Social Post</h2><div className="space-y-4"><div className="flex gap-3 flex-wrap">{['facebook', 'instagram', 'google', 'tiktok'].map(p => (<button key={p} onClick={() => setPostPlatforms(prev => prev.includes(p) ? prev.filter(x => x !== p) : [...prev, p])} className={`${btn} border-2 capitalize ${postPlatforms.includes(p) ? 'border-blue-500 bg-blue-500/10 text-blue-400' : 'border-white/10 text-gray-400'}`}>{p}</button>))}</div><textarea className={inp} rows={4} value={postText} onChange={e => setPostText(e.target.value)} placeholder="What's happening at Alpha International today?" /><div className="flex items-center gap-3"><input ref={fileInputRef} type="file" multiple accept="image/*,video/*" className="hidden" onChange={e => { if (e.target.files) setPostMedia(Array.from(e.target.files)) }} /><button onClick={() => fileInputRef.current?.click()} className={btnS}>Add Photos/Videos ({postMedia.length})</button><button onClick={createSocialPost} disabled={posting} className={`${btnP} ml-auto`}>{posting ? 'Posting...' : 'Post Now'}</button></div></div></div>{postHistory.length > 0 && <div className={card}><h3 className="font-bold text-white mb-3">Post History</h3><div className="space-y-2">{postHistory.map((p, i) => (<div key={i} className="p-3 bg-[#0f1923] rounded-xl"><div className="flex justify-between"><span className="text-sm text-white font-medium">{p.platforms?.join(', ')}</span><span className="text-xs text-gray-500">{timeAgo(p.created_at)}</span></div><p className="text-xs text-gray-400 mt-1 line-clamp-2">{p.text}</p></div>))}</div></div>}</div>}

{tab === 'capture' && <div className="space-y-6"><div className={card}><h2 className="text-xl font-bold text-white mb-5">Walk-in / Call Capture</h2><div className="grid md:grid-cols-2 gap-4"><div><label className={lbl}>Name *</label><input className={inp} value={captureName} onChange={e => setCaptureName(e.target.value)} placeholder="John Smith" /></div><div><label className={lbl}>Phone</label><input className={inp} value={capturePhone} onChange={e => setCapturePhone(e.target.value)} placeholder="(713) 555-0000" /></div><div><label className={lbl}>Service</label><input className={inp} value={captureService} onChange={e => setCaptureService(e.target.value)} placeholder="Oil change, brakes..." /></div><div><label className={lbl}>Source</label><select className={inp} value={captureSource} onChange={e => setCaptureSource(e.target.value)}><option value="walk-in">Walk-in</option><option value="phone">Phone Call</option><option value="facebook">Facebook</option><option value="google">Google</option></select></div></div><button onClick={captureLead} className={`${btnG} mt-5 w-full`}>Capture Lead</button></div></div>}

{tab === 'intel' && <div className="space-y-6"><div className={card}><h2 className="text-xl font-bold text-white mb-2">Smart Customer Search</h2><p className="text-sm text-gray-400 mb-5">Enter a name - AI investigates social media, phone, email.</p><div className="flex gap-3"><input className={inp} value={intelQuery} onChange={e => setIntelQuery(e.target.value)} placeholder="Enter name, business, or Google reviewer..." onKeyDown={e => e.key === 'Enter' && smartSearch()} /><button onClick={smartSearch} disabled={intelSearching} className={btnP}>{intelSearching ? 'Searching...' : 'Investigate'}</button></div></div>{intelResults && <div className={card}><h3 className="font-bold text-white mb-4">Results: {intelQuery}</h3><div className="space-y-3">{intelResults.results?.map((r: any, i: number) => (<div key={i} className="p-4 bg-[#0f1923] rounded-xl"><div className="flex items-center justify-between mb-2"><span className="text-sm font-semibold text-white">{r.source || 'Web'}</span><span className={`text-xs px-2 py-0.5 rounded ${r.confidence === 'high' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-yellow-500/20 text-yellow-400'}`}>{r.confidence || 'medium'}</span></div>{r.name && <p className="text-sm text-gray-300">Name: {r.name}</p>}{r.phone && <p className="text-sm text-blue-400">Phone: {r.phone}</p>}{r.email && <p className="text-sm text-gray-300">Email: {r.email}</p>}{r.social && <p className="text-sm text-purple-400">Social: {r.social}</p>}</div>)) || <p className="text-gray-500">No results found.</p>}</div>{intelResults.summary && <div className="mt-4 p-3 bg-blue-500/10 rounded-xl text-sm text-blue-300">{intelResults.summary}</div>}</div>}</div>}

{tab === 'ads' && <div className="space-y-6"><div className={card}><h2 className="text-xl font-bold text-white mb-5">Ad Campaigns</h2><div className="grid md:grid-cols-2 gap-4"><div className="p-6 border-2 border-dashed border-white/10 rounded-2xl text-center hover:border-blue-500/50 cursor-pointer" onClick={() => { setAdsPlatform('google'); setShowAdsModal(true) }}><h3 className="font-bold text-white text-lg">Google Ads</h3><p className="text-sm text-gray-400 mt-2">Target Houston searches</p><button className={`${btnP} mt-4`}>Create Campaign</button></div><div className="p-6 border-2 border-dashed border-white/10 rounded-2xl text-center hover:border-blue-500/50 cursor-pointer" onClick={() => { setAdsPlatform('facebook'); setShowAdsModal(true) }}><h3 className="font-bold text-white text-lg">Facebook Ads</h3><p className="text-sm text-gray-400 mt-2">Target Houston drivers</p><button className={`${btnP} mt-4`}>Create Campaign</button></div></div></div>{campaigns.length > 0 && <div className={card}><h3 className="font-bold text-white mb-4">Campaigns ({campaigns.length})</h3><div className="space-y-2">{campaigns.map(c => (<div key={c.id} className="flex items-center justify-between p-4 bg-[#0f1923] rounded-xl"><div><p className="font-semibold text-white">{c.name}</p><p className="text-xs text-gray-500">{c.platform} - <span className={`px-2 py-0.5 rounded text-xs font-semibold ${c.status === 'active' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-yellow-500/20 text-yellow-400'}`}>{c.status}</span></p></div><span className="text-sm text-gray-300">${c.spend || 0}</span></div>))}</div></div>}</div>}

</div>)
}